apiVersion: v1
kind: Service
metadata:
  name: postgres-flag-service-deployment
  namespace: toggle-master
  labels:
    app: postgres-flag-service-deployment
spec:
  type: ClusterIP
  selector:
    app: postgres-flag-service-deployment
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
